/**
 * Point d'entree du serveur.
 *
 * C'est ici que vivent toutes les entrees-sorties: Express, Socket.IO, l'horloge
 * et, plus tard, la base de donnees. Le serveur appelle le moteur de
 * @neon-ninja/sim en lui injectant le temps et la graine.
 *
 * Le contenu reel (GameRoom, RoomManager, couche Socket.IO) arrive aux
 * etapes 2.1 et 2.2.
 */

export {};
