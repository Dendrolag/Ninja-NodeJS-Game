/**
 * Fonction de demonstration de l'etape 0.1.
 *
 * Elle n'a aucun rapport avec le jeu. Elle existe uniquement pour donner au
 * harnais de test quelque chose de reel a verifier, et prouver que la chaine
 * compilation, linter, tests et integration continue fonctionne de bout en bout.
 *
 * A supprimer des que packages/shared contiendra du vrai code (etape 1.1).
 */
export function additionner(a: number, b: number): number {
  return a + b;
}
