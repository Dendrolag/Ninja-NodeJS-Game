/**
 * Point d'entree du paquet partage.
 *
 * Ce paquet contient ce que le moteur, le serveur et le client ont en commun:
 * les constantes de jeu, les types partages, le generateur de nombres a graine,
 * et les schemas de validation des entrees.
 *
 * A ce stade (etape 0.1) il ne contient qu'une fonction de demonstration, dont
 * le seul role est de prouver que la chaine de compilation et de test tourne.
 * Le contenu reel arrive a l'etape 1.1.
 */

export { additionner } from './demo.js';
